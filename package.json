{
  "private": true,
  "scripts": {
    "lint": "npx prettier --check tests/",
    "lint:fix": "npx prettier --write tests/",
    "test": "npx anchor test",
    "submodules": "git submodule update --init --recursive && (cd goki && yarn && yarn prepare)",
    "build:deps": "yarn build:deps:notest && (cd goki && yarn test:e2e)",
    "build:deps:notest": "(cd goki && anchor build && yarn idl:generate) && mkdir -p deps && cp goki/target/types/*.ts deps && cp goki/target/deploy/*.so deps"
  },
  "devDependencies": {
    "@metaplex-foundation/amman": "^0.5.0",
    "@project-serum/anchor": "^0.24.2",
    "@project-serum/anchor-cli": "^0.24.2",
    "@solana/spl-token": "^0.2.0",
    "@types/bn.js": "^5.1.0",
    "@types/mocha": "^9.1.0",
    "eslint": "^8.13.0",
    "mocha": "^9.1.3",
    "prettier": "^2.6.2",
    "ts-mocha": "^9.0.2",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "@solana/web3.js": "^1.38.0",
    "bn.js": "^5.2.0"
  }
}
